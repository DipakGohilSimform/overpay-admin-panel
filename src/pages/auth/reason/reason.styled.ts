import styled from 'styled-components'

export const Container = styled.div`
  .reasonsWrapper {
    display: flex;
    gap: 24px;
    .walletIcon {
      color: var(--portage);
    }
    .drivingIcon {
      color: var(--orange);
    }
    .moneysendIcon {
      color: var(--yellow);
    }
    .statusUpIcon {
      color: var(--green);
    }
  }
`
