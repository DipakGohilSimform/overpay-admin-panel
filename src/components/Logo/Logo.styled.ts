import styled from 'styled-components'

export const LightLightLogo = styled.a`
  display: flex;
  align-items: center;
  gap: 6px;
  height: 32px;
  width: 149px;
  span {
    color: var(--neutral-0);
    font-size: 26px;
    font-weight: 600;
  }
`

export const DarkLightLogo = styled.a`
  display: flex;
  align-items: center;
  gap: 6px;
  height: 32px;
  width: 149px;
  span {
    color: var(--neutral-0);
    font-size: 26px;
    font-weight: 600;
  }
`

export const DarkDarkLogo = styled.a`
  display: flex;
  align-items: center;
  gap: 6px;
  height: 32px;
  width: 149px;
  span {
    color: var(--neutral-900);
    font-size: 26px;
    font-weight: 600;
  }
`
